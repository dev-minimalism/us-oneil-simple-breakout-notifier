[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "us-oneil-simple"
version = "3.0.0"
description = "William O'Neil Breakout Trading Bot - CAN SLIM strategy pattern detection for US stocks"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Yungoo Park", email = "ygpark@lendingmachine.co.kr"}
]
keywords = [
    "trading",
    "stock",
    "breakout",
    "canslim",
    "william-oneil",
    "telegram-bot",
    "technical-analysis"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Financial and Insurance Industry",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Office/Business :: Financial :: Investment",
]
requires-python = ">=3.12"
dependencies = [
    "yfinance>=0.2.40",
    "pykrx>=1.0.45",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
    "psycopg2-binary>=2.9.9",
    "sshtunnel>=0.4.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
]

[project.urls]
Homepage = "https://github.com/dev-minimalism/oneil-breakout"
Repository = "https://github.com/dev-minimalism/oneil-breakout"
Issues = "https://github.com/dev-minimalism/oneil-breakout/issues"

[project.scripts]
us-oneil-simple = "us_oneil_simple.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
us_oneil_simple = ["py.typed"]

[tool.black]
line-length = 100
target-version = ["py312"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_ignores = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --cov=us_oneil_simple --cov-report=term-missing"